```mermaid
classDiagram
%% 命名空间 xr

classDiagram
    class xr::Solver {
        +solve(config, vio_debug_data)
        +marginalize(solve_config, marg_config, vio_debug_data)
        +generateNewMarginPrior(marg_config)
        +checkMarginPriorNullSpace(verbose)
        +checkMargEigenvalues(verbose)
        +reset(t_ns, cam_idx_order)
        +setFixState(fix_ex, fix_in)
        +get_marg_prior_H_or_J()
        +get_marg_prior_b_or_r()
        +get_marg_prior_order_()
        ...
    }

    xr::Solver --> VioSolveData : uses in SolveConfig
    xr::Solver --> VioCalib : calib_
    xr::Solver --> EliminatorBase : eliminator_
    xr::Solver --> LandmarkDatabase : uses in SolveConfig
    xr::Solver --> IntegratedImuMeasurement : uses in SolveConfig
    xr::Solver --> VariableBase : VariableLookupTable
    xr::Solver --> FactorBase : FactorSet
    xr::Solver --> RplCache : rpl_cache_

```