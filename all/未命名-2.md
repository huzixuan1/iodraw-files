```mermaid
sequenceDiagram
    autonumber
    participant vioFront
    participant VioApi
    participant VioOptimization
    participant SqrtKeypointVio
    participant Solver

    App/User ->> VioApi: process(track_res, detect_res, imu_pack, output)
    VioApi ->> VioOptimization: process(track_res, detect_res, imu_pack, output)
    VioOptimization ->> SqrtKeypointVio: process(imu_pack, track_res, output)
    SqrtKeypointVio ->> SqrtKeypointVio: vioUpdate(output)
    SqrtKeypointVio ->> Solver: solve(config, vio_debug_data)

```