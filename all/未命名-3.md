```mermaid
1. 设置初始状态：output.status = SLAM_STATUS_INIT；
2. 若无 IMU 数据直接返回；
3. 转换当前帧时间戳 & 视觉观测结果；
4. 初始化 vio 状态 vioInitialize()，未初始化返回；
5. 启用 debug 数据结构；
6. 判断是否需要进入 VIO 优化流程（通过 ZUPT 检测）；
   - 如果需要：
     7.1 IMU预积分处理 processImuMeas()
     7.2 视觉信息处理（Landmark 跟踪、关键帧选择）processVisionMeas()
     7.3 优化滑窗 vioUpdate()
8. 状态后处理（赋值输出 imu 状态、更新 prev 变量等）；
9. 输出 debug 及日志信息。

```